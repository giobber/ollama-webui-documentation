name: ${PROJECT_NAME:-llm4proposal}

services:
  ollama:
    container_name: ${PROJECT_NAME:-llm4proposal}_ollama
    env_file: .env
    image: ollama/ollama
    restart: unless-stopped
    # tty: true
    ports:
      - "${OLLAMA_PORT:-11434}:11434"
    volumes:
      - ollama:/root/.ollama
    # This section is required to enable GPU usage
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  open-webui:
    container_name: ${PROJECT_NAME:-llm4proposal}_open-webui
    env_file: .env
    image: ghcr.io/open-webui/open-webui:cuda
    restart: unless-stopped
    ports:
      - "${WEBUI_PORT:-3000}:8080"
    volumes:
      - open-webui:/app/backend/data
    environment:
      - OLLAMA_BASE_URLS=http://ollama:11434
    # This section is required to enable GPU usage
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

volumes:
  open-webui:
    name: ${PROJECT_NAME:-llm4proposal}_open-webui
  ollama:
    name: ${PROJECT_NAME:-llm4proposal}_ollama
