name: llm4proposal

services:
  open-webui:
    container_name: llm4proposal_open-webui
    env_file: .env
    image: ghcr.io/open-webui/open-webui:cuda
    restart: unless-stopped
    ports:
      - "${WEBUI_PORT:-3000}:8080"
    volumes:
      - open-webui:/app/backend/data
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

volumes:
  open-webui:
